/** Shared so the nav and the page title can't drift out of sync. */
export const NAV_ITEMS = [
  { label: "Home", href: "/drag-nav/a" },
  { label: "About", href: "/drag-nav/b" },
  { label: "Contact", href: "/drag-nav/c" },
] as const;
